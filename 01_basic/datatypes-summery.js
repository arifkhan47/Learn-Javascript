// there are 2 categories of datatypes 1. premetive    2. Non-premetive

// premetive datatypes

// nn bb ss u

// null    Number    Boolean    BigInt    String     Symbol   undefined

let temprature = null
const age = 26
const userLoggesIn = yes
const UserId = BigInt(324354634587654)
const name = "Arif Khan"
const userSymbol = Symbol('76543')
const status = undefined



// Non - premetives

// Array Object function

const day = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"]

const user = {
    name :"arif",
    age : 26
}


const myFunction = function () {
    console.log(user.name)
}